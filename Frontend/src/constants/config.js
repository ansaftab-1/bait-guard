/**
 * Application Configuration & Feature Flags
 */

export const APP_CONFIG = {
  // Set to false to disable external backend API network calls and rely purely on client-side mock data & localStorage
  ENABLE_BACKEND: false,
  USE_MOCK_DATA: true,
  MOCK_DELAY_MS: 300,
}
